import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReservaTuCochera2Page } from './reserva-tu-cochera2.page';

describe('ReservaTuCochera2Page', () => {
  let component: ReservaTuCochera2Page;
  let fixture: ComponentFixture<ReservaTuCochera2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ReservaTuCochera2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
